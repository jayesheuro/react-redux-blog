const initialState = {
  blogs: [],
  blog: {},
  loading: false,
};

const blogsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default blogsReducer;
